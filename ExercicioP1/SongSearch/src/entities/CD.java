package entities;

public class CD extends Song{
    
    public CD(String title, String album_name, String composer, String interpreter, int year) {
        super(title, album_name, composer, interpreter, year);
    }
}
